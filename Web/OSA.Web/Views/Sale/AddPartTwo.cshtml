@model OSA.Web.ViewModels.Sales.Input_Models.CreateSaleInputModelTwo
@{
    this.ViewData["Title"] = "Add Sale";
}

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <h3>Месечни Продажби</h3>
            <partial name="_FlashMessageTypePartial" />
            <div class="form-group">
                <label asp-for="StockName"></label>
                <select id="dropdowntipo" asp-for="StockName" class="form-control">
                    <option value="0" hidden disabled selected>Избери стока</option>
                    @foreach (var name in Model.StockNames)
                    {
                        <option>@name</option>
                    }
                </select>
                <span asp-validation-for="StockName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TotalPrice"></label>
                <input asp-for="TotalPrice" class="form-control">
                <span asp-validation-for="TotalPrice" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProfitPercent"></label>
                <input asp-for="ProfitPercent" class="form-control">
                <span asp-validation-for="ProfitPercent" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StartDate" class="control-label"></label>
                <input asp-for="StartDate" id="picker" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script type="text/javascript">
        $(".picker").datetimepicker({
            format: "dd/MM/yyyy",
            autoclose: true,
            todayBtn: true,
            pickerPosition: "bottom-left"
        });
    </script>
}
